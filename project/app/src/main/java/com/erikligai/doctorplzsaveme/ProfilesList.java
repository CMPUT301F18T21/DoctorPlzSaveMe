package com.erikligai.doctorplzsaveme;

public class ProfilesList {

    /**
     * Add profile to ProfilesList
     * @param profile: profile that is added
     * @return Nothing
     */
    public void add(Profile patient) {
    }

     /**
     * Returns if ProfilesList has given profile or not
     * @param patient: we want to check if profile exists in ProfiesList
     * @return boolean that returns true if profile is in ProfilesList, false if not
     */
    public boolean hasProfile(Profile careProvider) {
        return true;
    }
}
